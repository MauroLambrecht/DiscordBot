const { Client, Interaction } = require("discord.js");

/**
 * Handle the "CreateTheme" subcommand for the "utils" command.
 *
 * @param {Client} client - The Discord.js client.
 * @param {Interaction} interaction - The interaction triggered by the user.
 * @param {String} promt - The promt a user has given.
 */
async function handleCreateTheme(client, interaction, promt) {}

module.exports = handleCreateTheme;
